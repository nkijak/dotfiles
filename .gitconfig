[user]
	email = nick.kijak@smartthings.com
	name = Nick Kijak
[credential]
	helper = cache --timeout=3600
[push]
	default = simple
[pull]
	rebase = false
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[diff]
	external = difft
